# Frontend Environment Variables

# API Base URL
# For local development:
VITE_API_BASE_URL=/api

# For production (Azure):
# VITE_API_BASE_URL=/api

# OpenID Connect Configuration
VITE_OPENID_CLIENT_ID=mindx-onboarding

# Optional: force IdP to show login screen (OIDC `prompt` parameter)
# Common values: login | none
VITE_OPENID_PROMPT=login

# NOTE: Redirect URI is automatically determined from window.location.origin
# Make sure the following redirect URIs are whitelisted on id-dev.mindx.edu.vn:
# - For local Docker: http://localhost:8080/auth/callback
# - For local Vite dev: http://localhost:5173/auth/callback
# - For Azure Web App: https://your-app.azurewebsites.net/auth/callback
# - For custom domain: https://your-domain.com/auth/callback

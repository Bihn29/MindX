# Backend Environment Variables

# Server Configuration
PORT=3000
NODE_ENV=development

# OpenID Connect Configuration
OPENID_CLIENT_ID=mindx-onboarding

# IMPORTANT: Only set OPENID_CLIENT_SECRET if your app is a confidential client
# For public clients with PKCE (most SPAs), leave this empty or don't set it
# OPENID_CLIENT_SECRET=

# For confidential clients only:
# OPENID_CLIENT_SECRET=your_secret_here
